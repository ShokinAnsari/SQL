# SQL
DDL Commands-> commands which defines or modify structure of tables, indexes and schema (create,alter,drop,truncate)

What are Constraints?
Constraints are rules applies to column to ensure data integrity and accuracy. It ensures that only valid data should be entered in Database.
Popular constraints (UNIQUE, NOT NULL,PRIMARY KEY,DEFAULT,CHECK)
